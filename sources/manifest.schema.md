# Source Manifest

`manifest.csv` records SHA256 hashes for files under `data/raw/`. Run `python scripts/inspect_raw_data.py` after receipt and `python scripts/validate_data.py` before each processing run. A changed hash requires review; raw files are immutable inputs.

