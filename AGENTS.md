# 项目工程规范

## 项目目标

本项目服务于“全国本科院校税收风险管控案例大赛”。目标是建立真实业务、真实数据、数据勾稽、风险指标、异常、风险假设、假阳性排除、证据核验、税法核验、风险确认、风险分级、整改和长效监控的可复现研究基础设施，而不是开发炫技 AI 产品。

## 真实性与结论门槛

- 严禁虚构企业、经营事实、财务/申报数据、合同、发票、流水、订单、访谈、政策或行业统计。
- 缺原始材料输出 `TODO_MISSING_DATA`；税务处理不确定输出 `NEED_TAX_REVIEW`；政策未核验输出 `POLICY_UNVERIFIED`。
- 异常只能标记 `ANOMALY`，风险必须按 `OBSERVATION -> ANOMALY -> POTENTIAL_RISK -> EVIDENCE_CHECK -> FALSE_POSITIVE_CHECK -> CONFIRMED_RISK` 推进。
- 所有数字由代码产生，报告/Excel/图表/PPT 的核心数字必须来自同一机器结果；不得手改计算结果。

## 数据要求

`data/raw/` 只读，代码不得修改。加工数据写入 `data/interim/` 或 `data/processed/`，计算结果写入 `outputs/`。测试夹具必须标记 `SYNTHETIC_TEST_DATA_ONLY`。

## 税法与证据

每条政策记录来源、发布日期、有效期、适用范围和核验日期，最终依据优先使用政府官方网站。风险必须可追踪到原始文件、字段、公式、政策和证据。

## 开发与写作

税额、比率、勾稽关系、风险分数、风险金额均需测试。禁止空话、概念堆砌、虚构信息或让 LLM 自行创造数字；数据不足时使用确定性规则和统计异常，不强行使用机器学习。

